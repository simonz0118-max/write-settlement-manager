# V5.3.1 Hotfix

- Fixed fatal browser parse error in `setLineValue()`.
- Broken actual line breaks in `.split()` / `.join()` replaced with JavaScript `\n` escape sequences.
- Added `window.__WRITE_APP_READY__` boot marker.
- UI and accounting export logic unchanged from V5.3.0.
