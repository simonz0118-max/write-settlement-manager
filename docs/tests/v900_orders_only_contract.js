const fs=require('fs'),assert=require('assert');const c=JSON.parse(fs.readFileSync(process.argv[2],'utf8'));assert.equal(c.productionInput,'ORDERS_ONLY');assert.equal(c.factInputRequired,false);assert.equal(c.singleSourceOfTruth,'ORDER_DATA');assert(c.unknownPolicy.mustAppearInFact);assert.equal(c.unknownPolicy.reviewMark,'RED_FONT_ONLY');assert.equal(c.unknownPolicy.literalReviewTextInFact,false);assert(c.facts.phantomRowsForbidden);assert(c.formalFactTakeover);console.log('V9 orders-only production contract PASS');