const fs=require('fs'),vm=require('vm'),assert=require('assert');const s=fs.readFileSync(process.argv[2],'utf8'),c={window:null};c.window=c;vm.createContext(c);vm.runInContext(s,c);const P=c.WRITE_V10_PRICING,line={origin:'CN',destinationCountry:'FRANCE',currency:'EUR',invoiceEntity:'DEFAULT',role:'PACKAGE',configurationFingerprint:'X'};let r=P.resolve(line,{currentContracts:[{ruleId:'C1',origin:'CN',destinationCountry:'FRANCE',currency:'EUR',configurationFingerprint:'X',unitTotal:12}]});assert.equal(r.unitTotal,12);r=P.resolve({...line,currency:'USD'},{currentContracts:[{ruleId:'C1',origin:'CN',destinationCountry:'FRANCE',currency:'EUR',configurationFingerprint:'X',unitTotal:12}]});assert.equal(r.status,'PRICE_PENDING');r=P.resolve(line,{unknownFallbacks:[{ruleId:'F1',approved:true,currency:'EUR',unitTotal:9,maxUnitTotal:10}]},P.MODE.ALWAYS_ISSUE);assert.equal(r.status,'PRICED_FALLBACK');console.log('V10 Pricing Waterfall strict currency/effective isolation PASS');