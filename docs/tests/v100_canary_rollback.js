const fs=require('fs'),vm=require('vm'),assert=require('assert');const s=fs.readFileSync(process.argv[2],'utf8'),store={},c={window:null,localStorage:{getItem:k=>store[k]||null,setItem:(k,v)=>store[k]=String(v)}};c.window=c;vm.createContext(c);vm.runInContext(s,c);const R=c.WRITE_V10_CANARY;R.setPercent(25);assert.equal(R.getPercent(),25);assert.equal(R.shouldUseV10({hardPass:false},'x').useV10,false);R.rollback();assert.equal(R.getPercent(),0);console.log('V10 canary rollback PASS');