WRITE Settlement Manager V10.0.2 最终修正版

只需要双击：
  一键部署V10.0.2最终修正版.command

本包修复三项：
1. 修复旧部署脚本把 V10.0.2 错误检查为 data-release="10.0.1" 的假失败。
2. 保留 V10.0.2 FACT 导出动态 cellXfs 样式基线修复，不再硬编码 style 89。
3. 保留生产 UI 清理，旧 V8/V9 Shadow 面板不再污染首页。

脚本会自动：
本地测试 → 版本一致性 → GitHub main → 二次测试 → Git push → Cloudflare Pages → pages.dev 与 f.neovora.co 双重线上验证。

如需只检查部署包、不真正部署，可在终端运行：
WRITE_DEPLOY_DRY_RUN=1 WRITE_NONINTERACTIVE=1 ./一键部署V10.0.2最终修正版.command
