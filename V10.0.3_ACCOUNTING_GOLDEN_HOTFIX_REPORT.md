# WRITE Settlement Manager V10.0.3 — Accounting Golden Compiler Hotfix

## 根因
V10.0.2 最终 FACT identity 错误包含 raw SKU，导致相同人工会计 Description 因 SKU 差异被拆成多行；SERVICE/FEE 也可能重新进入 package composition；营销折扣片段可能进入正式 Description。

## V10.0.3 修复
- 最终 PACKAGE 唯一键改为 invoiceEntity + origin + country + currency + taxRegime + role + 完整会计 configuration。
- raw SKU 只保留在 provenance，不再参与最终人工 FACT 聚合。
- PACKAGE 内相同会计组件先合并 multiplicity，再形成 configuration fingerprint。
- SERVICE / FEE / UPSELL 独立聚合，不再塞回 PACKAGE Description。
- `47% d'économie` / discount / save 等纯营销片段只保留审计，不生成收费组件。
- CN / FR、EUR / USD / GBP 不跨维度合并。
- 新增 duplicateFinalRows / finalAggregationPass 硬门；最终相同会计键出现两行直接 hardPass=false。
- Golden FACT 模板、红字审核、未知商品不遗漏、V9 rollback 均保留。

## 实际回归
- V10 production contract: PASS
- V10.0.3 accounting golden final aggregation: PASS
- V8.2 Zero-Loss: 162 orders / 260 items / 289 qty PASS
- V8.3 Classification Fidelity: 49/49 PASS
- V8.3.1 Trace Fidelity: 49/49 PASS
- V10 15 semantic scenarios: 15/15 PASS
- V10 50k Accounting IR seed 539363346: hardPass=true
- V10 production stress: missingSourceItems=0

## 生产原则
最终 FACT 是会计汇总，不是订单明细。底层 source/order/item/atom 全量保留在 audit/provenance；正式 FACT 只输出人工会计意义上唯一的汇总行。
