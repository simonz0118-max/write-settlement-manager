# WRITE Settlement Manager

## 当前版本 — v6.5.4

- 每个新版本首次在同一浏览器打开时自动显示更新日志，阅读关闭后本版本不再重复弹出。
- 保留“自动 / 浅色 / 深色”三态主题，并记住手动选择。
- `CHANGELOG.md` 正式成为每次发布必须同步更新的文件。
- UI、Git commit、Cloudflare 部署日志与 RELEASE 版本号统一。

完整版本历史请查看 [CHANGELOG.md](./CHANGELOG.md)。

— Milestone 2

## 启动
Mac 双击 `start.command`，浏览器打开 `http://localhost:4173`。

## 已完成
- 稳定流式导入 `.xlsx` / `.zip`，继续规避超大 Sheet XML 内存爆炸。
- 只忽略 `FACT` Sheet，不忽略同工作簿中的真实订单 Sheet。
- 订单号去重。
- 自动识别：铅笔、雕刻服务、普通笔芯、彩色笔芯、礼盒、礼品卡、B2B。
- `🎁` / `100% off` 自动标记为赠品。
- 订单级会计分类为互斥分类，因此分类金额可以直接相加并与总订单金额对账。
- 商品级分类用于商品件数/赠品件数统计；不把订单金额强行分摊到每件商品。
- 未识别商品保留为“待确认”，同时生成待复核清单，避免错误硬分类。
- 一键导出 `.xlsx`，包含：`会计汇总`、`订单明细`、`商品分类明细`、`导入日志`。

## Milestone 3
下一阶段解析 FACT 中的 COGs / Shipping 表，把成本与物流规则自动匹配到订单，生成毛利与差异检查。


## v4.1 更新

- 重画原创白线侧脸熊猫 Logo：双圆耳、眼斑、鼻口轮廓更明确。
- 取消 1060px 强制最小宽度，优化 900–1100px 浏览器窗口，避免仪表盘整体横向滚动。
- Excel 导出重构为 6 张专业工作表：结算总览、订单明细、商品汇总、待复核、订单审计、导入日志。
- 修复数量列误显示 €、百分比误放大等数字格式问题。
- 订单明细只保留结算常用字段，来源文件/Sheet/源行号移至审计表，降低视觉噪音。


## v4.1 更新
- 重新绘制原创白线侧脸熊猫 Logo，强化耳朵、眼斑、吻部和鼻口特征。
- 保留并验证专业六层会计导出结构。
