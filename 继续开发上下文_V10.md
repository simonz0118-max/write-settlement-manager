# WRITE Settlement Manager V10 — 当前开发状态

当前状态：NEEDS WORK（V10 尚未生产接管）
正式生产：V9.0 保持不变。

已完成并实际测试：
- P0-1：cadeau 不再自动免费；付费礼盒、付费样品、显式免费证据分离。
- P0-2：BillableAtom 多实体解析；单格支持 2/3/4 组件及倍数。
- P0-3：PACKAGE / UPSELL / SERVICE / FEE 独立 IR 行。
- P0-4：currency 进入 IR 聚合键；EUR/USD/GBP 不合并。
- P0-5：五重守恒硬门：订单、源商品、原子数量、计费语义、金额/币种。
- FR handling/processing fee 作为 FEE 独立落账。
- CanonicalOrder 基础层。
- Pricing Waterfall 基础层：STRICT_FORMAL / ALWAYS_ISSUE（只有 approved fallback 才允许兜底）。
- Universal Ingestion 基础：签名识别、CSV/TSV/JSON/XML、ZIP path traversal / bomb 限制。
- V8/V9 旧回归保持通过：162 / 260 / 289、49/49 Classification、49/49 Trace。

仍需完成：
- 52 份完整人工 FACT 全量 V10 replay。
- 3 份 partial 学习数=0 的新 V10 回归。
- 113 种香皂人工组合全量测试。
- PDF 表格、扫描件/OCR。
- 正式 PDF invoice compiler。
- 价格瀑布全链路及企业兜底配置 UI。
- 固定 seed 539363346 的 1k/10k/50k 语义压力。
- 浏览器真实导入、XLSX/PDF/audit JSON 导出。
- shadow/canary 5→25→50→100。
- GitHub/Cloudflare 发布。

外部阻塞：
当前 ChatGPT GitHub integration 可读 main，但 create_branch/create_file 返回 403 Resource not accessible by integration，因此本轮无法从此环境直接提交/推送。代码已在本地构建并可继续打包验证。
