(function(g){'use strict';
function panel(){let e=document.getElementById('v9ProductionPanel');if(e)return e;const p=document.querySelector('[data-view-panel="dashboard"] .dashboard-grid');if(!p)return null;e=document.createElement('section');e.id='v9ProductionPanel';e.className='panel wide v9-production-panel';e.innerHTML='<div class="panel-head"><div><h2>V9 生产完整性</h2><p>订单唯一事实源 · 新订单无需FACT</p></div><span id="v9ProdStatus">等待订单</span></div><div class="v8-audit-grid" id="v9ProdGrid"></div><div id="v9ProdNote"></div>';p.appendChild(e);return e}
async function run(){panel();const b=g.WRITE_V8_SOURCE_BRIDGE?.();if(!b?.orders?.length)return;const a=g.WRITE_V9_FACT_ENGINE?.auditAll?.()||[],orders=a.reduce((x,y)=>x+y.sourceOrders,0),items=a.reduce((x,y)=>x+y.sourceItems,0),tracked=a.reduce((x,y)=>x+y.trackedItems,0),lost=a.reduce((x,y)=>x+y.lostItems.length,0),review=a.reduce((x,y)=>x+y.reviewRows,0),t=await g.WRITE_V9_TEMPLATE?.verifyTemplate?.(),grid=document.getElementById('v9ProdGrid'),s=document.getElementById('v9ProdStatus'),n=document.getElementById('v9ProdNote');grid.innerHTML=[['源订单',orders],['源商品记录',items],['已追踪',tracked],['丢失',lost],['红字行',review],['Golden模板',t?.ok?'LOCKED':'FAIL']].map(([k,v])=>`<article><span>${k}</span><strong>${v}</strong></article>`).join('');s.textContent=lost===0&&t?.ok?'PRODUCTION READY':'CHECK';n.innerHTML=lost===0?'<div class="success-strip">✓ 未知商品仍进入FACT；缺价格留空；红字只改变Description字体颜色。</div>':'<div class="error-card" style="display:block">发现源商品追踪缺失。</div>'}
function start(){panel();window.addEventListener('write-import-complete',()=>setTimeout(run,450));setTimeout(run,900)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start);else start();g.WRITE_V9_PRODUCTION_AUDIT={VERSION:'9.0.0',run};
})(window);