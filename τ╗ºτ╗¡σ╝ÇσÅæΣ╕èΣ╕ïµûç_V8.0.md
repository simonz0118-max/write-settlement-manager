# WRITE Settlement Manager V8.0

V8.0 第一阶段 = Semantic Core + Shadow Engine。

## 不可破坏的生产约束
- V7.5.9 继续负责正式 FACT；V8.0 不替换 generatedGenericFactRowsForWorkbook。
- V8 未知商品、未知 Role、低置信度、规则冲突只进入待确认，绝不阻断 FACT。
- 源订单/源商品/SKU/数量/备注完整保留。

## V8 语义模型
- Product Family
- Role: MAIN / UPSELL / SERVICE / FEE / FREE_GIFT / STOCK / UNKNOWN
- Configuration Signature
- Quantity Strategy（按 Role）
- Description Strategy
- Rule Confidence / Evidence

## 规则优先级
人工修正 > 已验证人工FACT > 人工确认规则 > 已验证历史规则 > 高置信度系统推断 > 普通系统推断。
同级中更精确 Scope 优先；冲突规则保留，不静默覆盖。

## Rule Scope
CURRENT_ORDER / EXACT_SKU / EXACT_PRODUCT / PRODUCT_FAMILY / ORDER_CONTEXT / GLOBAL_PATTERN。

## Training Safety Gate
COMPLETE / PARTIAL / REFERENCE_ONLY / REJECTED。
只有 COMPLETE 可以成为自动训练真值；PARTIAL 不得污染规则库。

## 数据存储
V8 Rule Store 使用独立 IndexedDB `write-semantic-rules-v8`。D1 同步是可选增强；同步失败不得影响 FACT。

## UI
首页只新增 V8 语义审计 Shadow 区域：源订单、源商品、Shadow FACT、规则命中、自动推断、待确认、冲突、新 Family。
